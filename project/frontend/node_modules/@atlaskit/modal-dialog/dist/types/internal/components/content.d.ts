import { KeyboardOrMouseEvent, ModalDialogProps } from '../types';
declare type ForcedRequired = 'onClose';
interface ContentProps extends Omit<ModalDialogProps, ForcedRequired>, Required<Pick<ModalDialogProps, ForcedRequired>> {
    shouldScroll: boolean;
    headingId: string;
    onClose: (e: KeyboardOrMouseEvent) => void;
}
export default function Content(props: ContentProps): JSX.Element;
export {};
